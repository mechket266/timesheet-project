apiVersion: networking.k8s.io/v1
kind: Ingress
meta
  name: timesheet-ingress
spec:
  rules:
  - host: timesheet.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: timesheet-service
            port:
              number: 8082
